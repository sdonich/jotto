import React from 'react';
import { shallow } from 'enzyme';
// import { storeFactory, findByTestAttr } from '../../../test/testUtils';

// import InputSecretWord, { UnconnectedInputSecretWord } from './InputSecretWord.js';

// const setup = (initialState = {}) => {
//   const store = storeFactory(initialState);
//   return shallow(<InputSecretWord store={store} />).dive();
// }

describe('', () => {
  test('', () => {
    
  });
});

// describe('redux props', () => {
//   test('enterSecretWord() is a function prop', () => {
//     const wrapper = setup();
//     const enterSecretWordProp = wrapper.instance().props.enterSecretWord;
//     expect(enterSecretWordProp).toBeInstanceOf(Function);
//   });
// });

// describe('enterSecretWord() action creator call', () => {
//   let enterSecretWordMock;
//   let wrapper;
//   beforeEach(() => {
//     enterSecretWordMock = jest.fn();
//     const props = {
//       enterSecretWord: enterSecretWordMock
//     }
//     wrapper = shallow(<UnconnectedInputSecretWord {...props} />);
//     const 
//   });
// });